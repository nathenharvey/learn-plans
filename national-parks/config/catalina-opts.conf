{{~#if bind.database}}
{{~#each bind.database.members}}

  export CATALINA_OPTS="-DMONGODB_SERVICE_HOST={{bind.database.members.[0].address}} -DMONGODB_SERVICE_PORT={{cfg.database.port}}"

{{~/each}}
{{~/if}}

